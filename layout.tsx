import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-sf-pro',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Maker STEAM Horizonte Cero | Innovación Educativa',
  description: 'Delegación académica dedicada al desarrollo de competencias STEAM mediante protocolos de alta dirección y aeronáutica. SEEC 2027 - Space Exploration Educators Conference, Houston.',
  keywords: ['STEAM', 'educación', 'aeronáutica', 'innovación', 'SEEC 2027', 'Houston', 'delegación académica'],
  authors: [{ name: 'Mtra. Erika Corina Sánchez Pastrana' }],
  generator: 'v0.app',
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export const viewport: Viewport = {
  themeColor: [
    { media: '(prefers-color-scheme: light)', color: '#f5f7fa' },
    { media: '(prefers-color-scheme: dark)', color: '#1a1f2e' },
  ],
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="es" className="bg-background">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
